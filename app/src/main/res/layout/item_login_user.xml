<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:padding="6dip" >

    <TextView
        android:id="@+id/item_user_TextView_index"
        android:layout_width="40dp"
        android:layout_height="fill_parent"
        android:layout_alignParentBottom="true"
        android:layout_alignParentTop="true"
        android:layout_marginRight="6dip"
        android:background="@color/colorPrimary"
        android:textAlignment="center"
        android:textColor="#FFFFFF"
        android:gravity="center_horizontal|center_vertical"
        android:textSize="16sp"
        android:text="0"
        android:textStyle="bold"/>

    <TextView
        android:id="@+id/item_user_TextView_name"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_toRightOf="@id/item_user_TextView_index"
        android:layout_alignWithParentIfMissing="true"
        android:ellipsize="marquee"
        android:singleLine="true"
        android:textSize="16sp"
        android:text="item_user_TextView_name"
        android:textStyle="bold"/>

    <TextView
        android:id="@+id/item_user_TextView_exams"
        android:layout_width="fill_parent"
        android:layout_height="26dip"
        android:layout_alignParentRight="true"
        android:layout_alignParentBottom="true"
        android:layout_above="@id/item_user_TextView_name"
        android:layout_toRightOf="@id/item_user_TextView_index"
        android:gravity="center_vertical"
        android:text="item_user_TextView_exams"
        android:textSize="12sp"

        android:textStyle="bold|italic"/>
</RelativeLayout> 